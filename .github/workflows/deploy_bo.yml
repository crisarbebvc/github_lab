name: despliegue-bo
on:
    push:
        branches: [ main ]
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
        - name: Get Latest Code
          uses: actions/checkout@v3
        - name: FTP Deployer
          uses: SamKirkland/FTP-Deploy-Action@v4.3.4
          with:
            server: ftpupload.net
            username: if0_35607821
            password: qAZxb6Nx9k8Rm
            server-dir: "/tandem/"
            local-dir: "./tandem"
